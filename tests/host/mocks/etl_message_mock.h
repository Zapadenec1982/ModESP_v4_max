#pragma once
// ETL message stubs for host build (modules use ETL messages for driver comms)
// In host tests, driver communication is not tested — stubs are sufficient.
#include "driver_messages_mock.h"
